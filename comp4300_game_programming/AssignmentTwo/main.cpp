#include "main.h"

#include <SFML/Graphics.hpp>

#include <iostream>
#include <filesystem>
#include <fstream>
#include <vector>

#ifdef _WIN32
#include <Windows.h>
#endif

namespace fs = std::filesystem;

int main(int argc, char* argv[], char* env[])
{
    ((void)argc);
    ((void)argv);
    ((void)env);

    return 0;
}
